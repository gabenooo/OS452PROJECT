testcase 8:
    The content is exactly the same, the join just happens in a slightly different order, but return numbers are correct.

testcase 13:
    The second status returned is 230013 instead of 210013, which is roughtly 100k more than the previous, ours just takes slightly longer because of our implementation

testcase 25:
    The messages recived are correct to the testcase, they are just printed out in a slightly different order.

testcase 30: 
    The return values are the same, the join just happens in a slightly different order

testcase 31:
    The return values are the same, the join just happens in a slightly different order

testcase 46:
    The return values are the same, the join just happens in a slightly different order